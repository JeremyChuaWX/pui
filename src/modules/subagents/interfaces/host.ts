/**
 * Host Entry: reserved. The subagents Module has no host-side needs today —
 * registration is composed from the Extension (`pi.ts`) and the TUI consumes
 * parsed run state through the UI Entry (`ui.ts`).
 */
export {};
