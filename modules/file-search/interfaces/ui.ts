/**
 * UI Entry: the `@`-completion surface. The Controller resolves the module's
 * `fd` binary here to feed the TUI's file autocomplete provider.
 */
export { resolveFdBinary, type SystemBinary } from "../binaries.js";
